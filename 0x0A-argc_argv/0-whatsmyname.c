#include <stdio.h>

/**
 * main - the entry point
 *
 * @argc: the number of arguments
 * @argv: array of arguments
 *
 * Return: intger value 0
 */

int main(int argc, char **argv)
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
